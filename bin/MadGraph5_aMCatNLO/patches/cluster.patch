--- MG5_aMC_v2_3_3/madgraph/various/cluster.py	2016-01-25 10:06:14.472857158 -0500
+++ /scratch/anlevin/cluster.py	2016-01-25 10:03:08.000000000 -0500
@@ -107,10 +107,8 @@
         self.submitted_exes = [] #HTCaaS
         self.submitted_args = [] #HTCaaS
 
-        if 'cluster_queue' in opts:
-            self.cluster_queue = opts['cluster_queue']
-        else:
-            self.cluster_queue = 'madgraph'
+        self.cluster_queue = None
+
         if 'cluster_temp_path' in opts:
             self.temp_dir = opts['cluster_temp_path']
         else:
@@ -851,6 +849,7 @@
                   Initialdir = %(cwd)s
                   %(requirement)s
                   getenv=True
+                  accounting_group = group_t3mit
                   queue 1
                """
         
@@ -859,6 +858,11 @@
         else:
             requirement = ''
 
+        if requirement:
+            requirement += ' && (OpSysAndVer == "SL6") && (Arch == "X86_64")'    
+        else:
+            requirement = 'Requirements = (OpSysAndVer == "SL6") && (Arch == "X86_64")'
+
         if cwd is None:
             cwd = os.getcwd()
         if stdout is None:
@@ -927,6 +931,7 @@
                   Initialdir = %(cwd)s
                   %(requirement)s
                   getenv=True
+                  accounting_group = group_t3mit
                   queue 1
                """
         
@@ -935,6 +940,11 @@
         else:
             requirement = ''
 
+        if requirement:
+            requirement += ' && (OpSysAndVer == "SL6") && (Arch == "X86_64")'    
+        else:
+            requirement = 'Requirements = (OpSysAndVer == "SL6") && (Arch == "X86_64")'
+
         if cwd is None:
             cwd = os.getcwd()
         if stdout is None:
@@ -1009,7 +1019,7 @@
         if not self.submitted_ids:
             return 0, 0, 0, 0
         
-        packet = 15000
+        packet = 5000
         idle, run, fail = 0, 0, 0
         ongoing = []
         for i in range(1+(len(self.submitted_ids)-1)//packet):
